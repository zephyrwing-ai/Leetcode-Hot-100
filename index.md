这是一个算法的解题的思路方向
一、先给你一个可执行的「算法解题四步法（工程版）」

你原来的框架我给你“工程化改写”成这样一套 每题都能用的流程：

✅ Step 1：把题目翻译成“数据流 + 目标”

你每拿到一道题，先强制自己回答 3 个问题（不写代码）：

输入的数据结构是什么？（数组 / 链表 / 树 / 字符串 / 图）

输出是什么？（一个值 / 一组下标 / 一个布尔 / 一个结构）

目标是什么？
👉 是“找存在性”？
👉 是“求最值”？
👉 是“计数”？
👉 是“构造一个解”？

这一步是为了避免你一上来就想“怎么写 for 循环”，而是先看清问题类型。

✅ Step 2：识别“状态”和“是否需要记忆”

你原来写的是“系统必须维护哪些状态”，在算法题里可以具体化成：

我是否需要记住“过去的信息”？

需要 → 哈希表 / 集合 / 前缀和 / 计数数组

不需要 → 双指针 / 滑动窗口 / 纯遍历

这个题是否有“重复子问题”？

有 → 动态规划

没有 → 贪心 / 双指针 / BFS/DFS

这是算法选型的核心：
👉 需不需要“存状态”，决定你用不用额外空间。

✅ Step 3：找“不变量”（你给的第 3 点非常关键）

这是高手和新手的分水岭。
你每题都要问自己一句话：

在遍历 / 递归 / 迭代的过程中，我必须保证什么条件始终成立？

举几个你刷过题的例子：

Two Sum：

不变量：result 中存的永远是“已经遍历过的数”

滑动窗口：

不变量：窗口内的区间始终满足某个约束（如和 ≤ k）

二分查找：

不变量：目标值一定在 [left, right] 区间内

你如果找不到不变量，说明你还没想清楚“算法为什么正确”。

✅ Step 4：描述“状态如何演化”（这一步帮你从暴力走向最优）

把你原来的第 4 点具体化为：

每走一步：

我是扩展范围（如右指针右移）？

还是收缩范围（如左指针右移）？

哪一步是单向不可逆的？

比如：指针只会往前走，不会回退

比如：DFS 访问过的节点不会再访问

这一步的本质是：
👉 你是否在构建一个“线性可收敛”的过程，而不是反复试错？
